#include "main.h"
#include <stdio.h>

/**
 * mul - a function that multiplies two integers
 * @a: integer
 * @b: integer
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	printf("%d * %d\n", a, b);
	return (0);
}
