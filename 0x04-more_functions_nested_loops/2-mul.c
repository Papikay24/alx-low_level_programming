#include "main.h"
#include <stdio.h>

/**
 * mul - a function that multiplies two integers
 * @a: integer
 * @b: integer
 * Return: 0
 */

int mul(int a, int b)
{
	calc("%d * %d\n", a, b);
	return (0);
}
