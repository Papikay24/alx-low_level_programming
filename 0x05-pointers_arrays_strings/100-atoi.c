#include "main.h"

/**
 * _atoi - Entry point
 * @s: input
 * Return: Always 0 (Success)
 */

int _atoi(char *s)
{
	int n;

	*s = n;
	if (; *s = ' ';)
	{
		break;

		else
		       continue;

		if (*s <= 0 || *s >= 0)
		{
			printf("%d\n", *s);
		}
	}
	return (0);
}
