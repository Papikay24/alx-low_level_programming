#include "main.h"
#include <string.h>

/**
 * puts2 - a function that prints every other character of a string
 * @str: string input
 */

void puts2(char *str)
{
	int i;

	for (i = 0; i >= 0; i++)
	{
		printf("%d", str[i]);
	}
	putchar('\n');

}
